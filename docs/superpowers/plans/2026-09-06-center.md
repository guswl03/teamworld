# BoB-inspired Center Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development to implement the center task; controller owns independent UI typography and browser verification. No commits or pushes.

**Goal:** Replace the village presentation with a readable fictional IT education center and twelve curriculum-based mentor NPCs.

**Architecture:** Keep Phaser, movement/transport contracts, team IDs and adventure storage v1. Share center furniture/wall geometry with collision. Mentor data is static, separate from real connected players. Dialogs use existing modal/focus conventions.

**Tech Stack:** Existing Next/React/Phaser/TypeScript, node:test. No dependencies.

**Spec:** Approved in conversation 2026-09-06: lobby/reception, central lounge, classrooms/labs, project rooms, mentor zone; 12 named mentors; navy/teal ID-card UI; body 16px, secondary/menu >=14px, titles 24px. User said to proceed with all prepared work.

## Global Constraints

- No new dependencies, external AI calls, migration, commit, push or branch creation.
- Preserve current IT avatars/shortcuts and user changes. Use apply_patch.
- Center is fictional, inspired by the publicly listed Pangyo address; not a verified floor plan or official BoB service.
- Mentor names and topics come from user screenshots, not identity inference from photos. No impersonated quotations or invented credentials.
- Keep chat drafts/focus, key guards, adventure storage version and landmark IDs.
- Work in existing checkout: user explicitly requested main directly earlier; no new branch.

## Task 1: Center scene and mentor integration

**Files:** create src/game/center-model.ts, src/game/center-art.ts, src/game/mentors.ts, src/components/mentor-dialog.tsx, tests/center.test.ts; modify world-model.ts, world-art.ts, world-details.ts, create-world.ts, adventure-model.ts, adventure-panel.tsx, world-shell.tsx. Do not modify CSS/avatar.tsx (controller owns).

**Interfaces:** center geometry consumed by scene renderer and collision. Mentor records have id/name/topics/position/avatar visual variant. Mentor lookup accepts x/y and returns nearest mentor within interaction radius or undefined. Keep existing WorldSession transport APIs. Mentor dialog uses class `adventure-dialog mentor-dialog`, CSS to be supplied by controller. Add `mentor-directory` in guilds/place task with buttons to travel near mentors (never open remote conversation pretending proximity). Scene travel may extend with mentor/center targets without altering transport protocol.

- [x] Write and run focused failing tests: from lobby, flood-fill walkable grid reaches every team spawn, all 12 mentor interaction positions, quest NPC, chest, landmarks. Walls and desks reject standing; doors permit it. Mentor lookup is distance-limited and stable when boundaries overlap. Existing saved adventure restore preserves IDs and progression.

```ts
assert.equal(
  canStand(wall.x + wall.width / 2, wall.y + wall.height / 2, obstacles),
  false,
);
assert.equal(mentorAt({ x: -100, y: -100 }), undefined);
for (const target of targets)
  assert.ok(reachable(target), `unreachable ${target.name}`);
```

- [x] Implement model and tests green before renderer. Keep WORLD 1600x1120; use open central corridors, glass-walled study rooms, desk groups, stage/screen, lounge seating, reception and window plants. Do not put collisions invisible to renderer or decorative walls across movement routes. Use original Phaser pixel geometry, no new raster generation needed for furniture.
- [x] Draw all 12 mentors with distinguishable IT atlas frame/accessory/color combinations, clear nameplates and NPC labels; preserve legacy guide quest or restyle as center receptionist. No fake online indicators. Prioritize NPC/chest interactions and avoid overlap.
- [x] Curriculum mapping: 전상현 시스템 프로그래밍 (컴파일러/언어/메모리/자료구조/네트워크); 강대명 파일시스템 인터널 (MBR/GPT/FAT32/NTFS/EXT4); 김관영 Git/GitHub 실전 (fork/PR/협업); 김관영 차량보안 입문 (CAN/LIN/FlexRay/ECU); 김관영 Recon 기반 보안 취약점 탐지 및 분석 실습 (OSINT); 김주생 가상메모리와 페이지 테이블 (CR3/주소 변환), 보안 특허 개발; 김태홍 생성형 AI 보안 (LLM Gateway); 남성엽 개발자를 위한 오펜시브 시큐리티; 문재현 AI에 대한 이해 및 적용 (기초/알고리즘/라벨링/모델/Agentic AI); 박수현 생성형 AI를 활용한 웹서비스 개발; 장상근 바이브 엔지니어링; 한현상 퍼블릭 클라우드 보안 개발과 AI 보안; 이경문 Network Protocol (Ethernet/ARP/IP/TCP/UDP/Tunneling). 양혁재 appears in roster but not provided curriculum: show topic '담당 주제 확인 중', no invented assignment.
- [x] Dialog shows topic cards, short neutral learning summaries and preparation checklist based on supplied curriculum. Prominent text '제공된 커리큘럼 기반 게임 안내 · 실제 멘토의 발언이나 실시간 상담이 아닙니다'. Keep safe educational high-level scope.
- [x] Update village-facing strings to center vocabulary (reception/lab/lounge/kit); keep keys not displayed legacy. Preserve local quest rewards and avatar equipment. Increase map labels to >=16px and player names >=16px at base zoom; avoid overlap.
- [x] Run node node_modules/tsx/dist/cli.mjs --test tests/center.test.ts and pnpm test/typecheck. Report exact red/green evidence in docs/superpowers/plans/2026-09-06-center-report.md. No git writes.

## Task 2: Readable center UI (controller)

**Files:** src/app/world-workspace.css, src/app/globals.css, src/components/avatar.tsx.

- [x] Inspect browser computed fonts before changes: body/input <16, menu/secondary <14 reproduces report. Capture CSS/avatar dimensions.
- [x] Replace world skin palettes with navy/teal/pale blue surfaces; body 16, secondary/menu minimum14, title24. Keep all selectors scoped to world. Enlarge controls and avoid HUD/task overlap. Use responsive two-column avatars when needed; no horizontal overflow.
- [x] Replace inline avatar width/height with CSS custom property fallback to allow responsive rules; prevent header flex-column from stretching portraits.

```tsx
style={{ '--avatar-size': `${size}px` } as CSSProperties}
```

```css
.avatar-image {
  width: var(--avatar-size);
  height: var(--avatar-size);
  display: inline-flex;
  justify-content: center;
  align-items: center;
  flex-shrink: 0;
}
```

- [x] Browser verifies settings, chat, all dock windows, mentor dialog, focus and readable computed font sizes at desktop and narrow viewport. Preserve pixel art and canvas dimensions.

## Task 3: Integration and handoff

- [x] Independent task/code review against working diff; fix confirmed regressions.
- [x] Run formatter, pnpm test, pnpm typecheck, pnpm build. Browser inspect actual rendered center, movement, mentor E, Escape, Enter chat, doors/furniture, overflow.
- [x] Document fictional layout/source limitations, curriculum provenance, original procedural art and generated IT asset origin. Report any unavailable hosted/multiplayer verification. Do not push.

## Progress / preflight

| Tasks | Shared boundary                            | Resolution                                            |
| ----- | ------------------------------------------ | ----------------------------------------------------- |
| 1 / 2 | mentor-dialog and mentor-directory classes | 1 owns markup, 2 owns CSS; no shared file editing     |
| 1 / 3 | scene and interaction APIs                 | integration tests plus browser walkthrough            |
| 2 / 3 | responsive visual styles                   | computed font size and overflow checks                |
| 1     | model/render/collision coherence           | single geometry source; flood-fill test               |
| 2     | large fonts / fixed panels                 | scroll bodies, responsive grid; retain readable sizes |
| 3     | uncommitted user changes                   | inspect working diff, no commit/branch/push           |

## Final status

Tasks 1–3 complete. Independent review P2 additional-team collision fixed and re-reviewed. 37 tests pass; typecheck passes. Browser validated all twelve mentor dialogs, movement lock/return, chat draft preservation and readable responsive UI. New individual mentor images are explicitly deferred by the user; existing temporary variants remain. No commit/push/deployment.
